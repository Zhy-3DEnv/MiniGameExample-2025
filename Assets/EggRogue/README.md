## EggRogue 模块说明

本目录用于存放蛋黄人肉鸽游戏相关的所有资源和脚本，尽量与现有的 `FlappyBrid` 模块解耦。

### 目录建议

- `Scripts`：C# 脚本（角色、怪物、关卡、天赋、系统管理等）。
- `Prefabs`：角色、怪物、子弹、UI 等预制体。
- `UI`：UI 相关资源（Canvas、界面 Prefab、美术资源）。
- `Configs`：各类配置（ScriptableObject、表格导出的配置等）。

> 后续开发新功能时，优先在本目录下创建子目录和资源，避免与原有 FlappyBird 工程互相污染。

